{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "pino-pretty": "^13.1.3"
  },
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "worker": "bun run workers/event-processor.ts",
    "enqueue:simulation": "bun run scripts/enqueue-simulation.ts"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bullmq": "^6.0.6",
    "cors": "^2.8.6",
    "ioredis": "^6.0.0",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  }
}
